<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DigitalStudentTicket.Views.ScanResultStudentsListView"
             Visual="Material" Title="Список студентов">


    <StackLayout>
        <ListView x:Name="MyListView"
            ItemTapped="Handle_ItemTapped"
            CachingStrategy="RecycleElement" Margin="10">
            <!--Built in Cells-->
            <!--<ListView.ItemTemplate>
        <DataTemplate>
            <TextCell Text="{Binding .}" />
        </DataTemplate>
    </ListView.ItemTemplate>-->

            <!--Custom View Cells-->

            <ListView.ItemTemplate>
                <DataTemplate >
                    <ViewCell Height="100">
                        <StackLayout Margin="0, 10">
                            <Label Text="{Binding Text}" />
                            <Label Text="{Binding Detail}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <Button x:Name="scanNextBtn" Text="Сканировать далее" Clicked="scanNextBtn_Clicked"/>
    </StackLayout>  
</ContentPage>
